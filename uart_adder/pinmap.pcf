# Lattice iCE40 breakout board pin mapping for the UART Adder example

# UART TX Port
set_io --warn-no-port i_data B10  # UART RX
set_io --warn-no-port o_data B12  # UART TX

# Clock (12 MHz)
set_io --warn-no-port i_clk J3
